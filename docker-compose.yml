services:
  app:
    build: .
    image: ghcr.io/blodigel/audio-reactive-visualizer:local
    ports:
      - "8080:8080"
    volumes:
      - viz-data:/data
    environment:
      DATA_DIR: /data
    restart: unless-stopped

volumes:
  viz-data:
